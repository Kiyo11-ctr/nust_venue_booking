-- Create database
CREATE DATABASE NUST_Venue_Booking;
GO

USE NUST_Venue_Booking;
GO

-- Users table
CREATE TABLE Users (
    user_id INT IDENTITY(1,1) PRIMARY KEY,
    username NVARCHAR(100) NOT NULL UNIQUE,
    password_hash NVARCHAR(255) NOT NULL,
    role NVARCHAR(20) NOT NULL CHECK (role IN ('student', 'staff', 'admin')),
    created_at DATETIME DEFAULT GETDATE()
);

-- Venues table
CREATE TABLE Venues (
    venue_id INT IDENTITY(1,1) PRIMARY KEY,
    name NVARCHAR(200) NOT NULL,
    location NVARCHAR(200) NOT NULL,
    capacity INT NOT NULL,
    equipment NVARCHAR(500),
    is_available BIT DEFAULT 1
);

-- Bookings table
CREATE TABLE Bookings (
    booking_id INT IDENTITY(1,1) PRIMARY KEY,
    user_id INT NOT NULL,
    venue_id INT NOT NULL,
    booking_date DATE NOT NULL,
    start_time TIME NOT NULL,
    end_time TIME NOT NULL,
    purpose NVARCHAR(500) NOT NULL,
    status NVARCHAR(20) DEFAULT 'pending' CHECK (status IN ('pending', 'approved', 'rejected')),
    created_at DATETIME DEFAULT GETDATE(),
    FOREIGN KEY (user_id) REFERENCES Users(user_id) ON DELETE CASCADE,
    FOREIGN KEY (venue_id) REFERENCES Venues(venue_id) ON DELETE CASCADE
);

-- Insert sample venues
INSERT INTO Venues (name, location, capacity, equipment, is_available) VALUES
('Lecture Hall A', 'Main Campus', 200, 'Projector', 1),
('Computer Lab 1', 'Engineering Block', 50, 'Computers', 1),
('Seminar Room B', 'Library Wing', 40, 'Whiteboard', 1),
('Auditorium', 'Central Building', 500, 'Projector, Sound System', 1),
('Meeting Room C', 'Admin Block', 20, 'TV, Whiteboard', 1);

-- Insert sample users (password = "password123" for all - in real app use hashed passwords)
-- For demo, we'll use plain text (you should hash in production)
INSERT INTO Users (username, password_hash, role) VALUES
('admin1', 'password123', 'admin'),
('staff_john', 'password123', 'staff'),
('student_amy', 'password123', 'student');

-- Insert sample bookings
INSERT INTO Bookings (user_id, venue_id, booking_date, start_time, end_time, purpose, status) VALUES
(2, 1, '2026-04-15', '10:00', '12:00', 'Faculty meeting', 'approved'),
(3, 2, '2026-04-16', '14:00', '16:00', 'Lab session', 'pending');
GO
